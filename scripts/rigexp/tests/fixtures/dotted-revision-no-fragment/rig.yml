# Synthetic fixture: hwmv2's revision dot-normalization (design-log
# 2026-07-26d, extensions.cmake:1772) -- a dotted revision id ('1.5')
# constructs a fragment filename with the dot replaced by an underscore
# (dotted-revision-no-fragment_1_5_defconfig), never the literal dot. No
# such fragment exists here, so rule 10 fires naming the NORMALIZED
# filename -- proof the normalization happened, not just that rule 10
# still works.
rig:
  name: dotted-revision-no-fragment
  board: nucleo_f401re/stm32f401xe/rig
  revisions:
    default: 1
    list: [1, "1.5"]
  instances: []
