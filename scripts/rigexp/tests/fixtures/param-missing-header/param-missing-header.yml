# Synthetic fixture: a dt-includes: entry that names a header which does not
# exist on the include path — rule 6 (lang-dt-include). Rejected at expand
# time, before any token is looked at, naming the dirs that were searched.
dt-includes:
  - zephyr/dt-bindings/input/no-such-header.h
instances:
  - name: btn
    shield: grove_btn
    socket: grove_d2
    params:
      gb_key:
        zephyr,code: INPUT_KEY_0
