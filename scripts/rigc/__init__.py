"""rigc — the rig compiler, built from scratch against rigexp's frozen
golden corpus (claude/rigs/rigc-mission-brief.md). Nothing is implemented
yet: `python -m rigc ...` fails loudly (see __main__.py) so a differential
run under RIG_EXPAND_COMPILE=rigc reports "not implemented", never a
confusable ModuleNotFoundError or a silent fallback to rigexp."""
