# Synthetic fixture: a symbolic token assigned with no dt-includes: declared
# at all — rule 5 (lang-dt-include). Distinct from rule 4: there is no
# vocabulary to resolve against in the first place, so the diagnostic must say
# THAT rather than blaming the token.
instances:
  - name: btn
    shield: grove_btn
    socket: grove_d2
    params:
      gb_key:
        zephyr,code: INPUT_KEY_0
