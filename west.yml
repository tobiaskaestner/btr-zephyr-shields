manifest:
  version: "0.13"

  # If not otherwise specified, the projects below should be obtained
  # from the tiacsys remote.
  defaults:
    remote: tiacsys

  # "remotes" is a list of locations where git repositories are cloned
  # and fetched from.
  remotes:
    - name: tiacsys
      url-base: https://github.com/tiacsys

  # West-related configuration for the Bridle repository.
  self:
    # This repository should be cloned to workspace/bridle.
    path: btr-shields
    # This line configures west extensions which are currently
    # only for internal use by TIAC maintainers; import build
    # commands from Zephyr repo.
    west-commands: scripts/west-commands.yml

  projects:
    - name: zephyr
      # Pinned to the tip of tskr/zephyr-rigs (2026-07-28 rebase) for the
      # duration of the rigc differential build: any red golden is then ours
      # by construction. Un-pin back to the branch name at rigc cutover.
      revision: 8da5b3a0f60f7e8e06aa7b99bde818bb1affe2bd
      import: true
