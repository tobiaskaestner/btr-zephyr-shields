Arduino UNO Click Shield Testing
################################

The shield is a pure carrier: it plugs an ``arduino-r3`` socket and offers
two ``mikrobus`` sockets of its own. Promoted on its own it carries no
device, so this suite asserts only that the promotion configures and
builds.
